apiVersion: v1
kind: ConfigMap
metadata:
  name: apply-setters-fn-config
data:
  service-name: tokko-lummopages-web
  service-port: 3000
  target-port: 3000
  rule: Host(`lummo.buzz`) || Host(`www.lummo.buzz`)
  secret-name: something-1
